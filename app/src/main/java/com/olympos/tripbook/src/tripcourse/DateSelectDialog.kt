package com.olympos.tripbook.src.tripcourse

import android.content.Context
import com.olympos.tripbook.config.BaseDialog

class DateSelectDialog(context: Context) : BaseDialog(context) {

    override fun show(title: String, message: String, okMessage: String) {
//        super.show(title, message, okMessage)
    }
}